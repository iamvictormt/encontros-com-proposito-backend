datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id        Int      @id @default(autoincrement())
  fullName  String
  email     String?  @unique
  cpf       String?  @unique
  password  String
  createdAt DateTime @default(now())
}
